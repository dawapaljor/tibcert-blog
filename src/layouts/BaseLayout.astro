---
import BaseHead from '@/components/BaseHead'
import Header from '@/components/Header'
import Footer from '@/components/Footer'
import ProviderTheme from '@/components/ProviderTheme'
import ProviderAnimations from '@/components/ProviderAnimations'
import { SpeedInsights } from "@vercel/speed-insights/next"
const { title, description, image, articleDate } = Astro.props
---

<html lang='en' class='scroll-smooth'>
	<head>
		<BaseHead title={title} description={description} ogImage={image} articleDate={articleDate} />

		<ProviderTheme />
		<ProviderAnimations />
	</head>

	<body class='relative bg-gradient-to-bl from-slate-200 to-orange-200 text-stone-950  dark:text-white dark:bg-gradient-to-bl dark:from-slate-800 dark:to-red-900'>
		<main
			class='mx-auto sm:max-w-2xl sm:px-8 lg:px-0 antialiased md:max-w-7xl grid gap-12 mt-4'>
			<Header />
			<slot />
			<Footer />
		</main>
		<SpeedInsights />
	</body>
</html>
